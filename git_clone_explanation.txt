Git clone will clone a repository into a newly created directory and creates remote-tracking branches for each branch in the cloned repository. It also creates and checks out an initial brach forked from the cloned repository's currently active branch
